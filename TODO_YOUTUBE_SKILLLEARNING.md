# TODO: YouTube recommended videos in SkillLearning

- [x] Inspect current `SkillLearningPage.jsx` to see how videos are rendered.
- [ ] Add backend route for YouTube search (place router under `backend/app/routes/youtube_routes.py` and include it in `main.py`).
- [ ] Update `SkillLearningPage.jsx` to fetch recommended videos from `GET /api/youtube/search` based on selected topic.
- [ ] Keep existing mock/fallback videos if API fails.
- [ ] Ensure build passes (run frontend tests/lint if available).


